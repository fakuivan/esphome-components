esphome:
  name: freshness-cycle-test

host:

external_components:
  - source:
      type: local
      path: ../../components/
    components: [freshness]

freshness:
  - id: node_a
    timeout: 5s
    all:
      - node_b
  - id: node_b
    timeout: 5s
    all:
      - node_a